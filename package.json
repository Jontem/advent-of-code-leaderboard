{
  "private": true,
  "workspaces": ["src/client", "src/server"],
  "devDependencies": {
    "shelljs": "^0.7.8"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "docker build": "docker build -t jontem/advent-of-code-leaderboard .",
    "docker-publish": "docker push jontem/advent-of-code-leaderboard"
  }
}
